import React from 'react'
import {KEYS} from '../API/data'
import {sectionTitle} from '../utils/utils'

const ChillAboutMe = (): JSX.Element => {
  sectionTitle(KEYS.ABOUT_ME_CHILL)
  return <></>
}

export default ChillAboutMe
